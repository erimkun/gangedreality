# GangedReality — Hata ve Sorun Raporu (11.02.2026)
**Son güncelleme: 13.02.2026** — Düzeltilen sorunlar ✅ ile işaretlenmiştir.

Uygulamanın tüm kaynak kodları detaylı şekilde incelenmiş; tespit edilen hatalar, yanlış işleyen bölümler ve potansiyel sorunlar aşağıda önem derecesine göre sıralanmıştır.

---

## 🔴 KRİTİK — Derlenmez / Çalışmaz

### 1. ✅ `zipExporter.ts` — Tamamen Bozuk → **DÜZELTİLDİ**
- ~~**Duplicate değişken tanımı:** `const projectId` aynı scope'ta 2 kez tanımlanmış → TypeScript derleme hatası.~~ ✅
- ~~**`zip` değişkeni hiç tanımlanmamış:** `JSZip` import ediliyor ama `const zip = new JSZip()` satırı yok.~~ ✅ `const zip = new JSZip()` eklendi.
- ~~**`project.json` 4 kez yazılıyor:** Farklı verilerle 4 ayrı `zip.file('project.json', ...)` çağrısı var.~~ ✅ Tek seferde yazılıyor.
- ~~**Store state'ini mutate ediyor:** `projectData.project.assets.mainModel` ve `models[].url` export sırasında doğrudan değiştiriliyor.~~ ✅ `JSON.parse(JSON.stringify())` deep clone ile çözüldü.
- ~~**Missing closing brace:** `forEach` bloğu kapanmadan bir sonraki işleme geçiliyor.~~ ✅

**Dosya:** `src/utils/zipExporter.ts`

---

## 🟠 YÜKSEK — Crash, Veri Kaybı, Memory Leak

### 2. ✅ Circular Import: `types/index.ts` ↔ `useHotspotStore.ts` → **DÜZELTİLDİ**
~~`types/index.ts` dosyası `useHotspotStore.ts`'den `HotspotNode` ve `HotspotSettings` tiplerini import ediyor, store da `types`'dan import yapıyor.~~ ✅ Tip tanımları (`HotspotShape`, `HotspotNode`, `HotspotSettings`) `types/index.ts`'e taşındı, `useHotspotStore.ts` oradan import edip re-export ediyor.

**Dosya:** `src/types/index.ts`, `src/store/useHotspotStore.ts`

### 3. ✅ `DEBUG = true` Production'da Açık (4 dosyada) → **DÜZELTİLDİ (13.02.2026)**
~~Aşağıdaki dosyalarda `DEBUG` flag'i `true` olarak bırakılmış — her işlemde console.log çalışıyordu.~~ ✅ Tüm dosyalarda `DEBUG = false` olarak ayarlandı:
- `src/store/useInteractionsStore.ts`
- `src/store/useProjectStore.ts`
- `src/components/canvas/ModelRenderer.tsx`
- `src/hooks/useModelLoader.ts`

### 4. ✅ `updateEffects` — Kısmi Deep Merge Hatası → **DÜZELTİLDİ**
~~`useSceneStore.ts`'deki `updateEffects` yalnızca `ao` objesini deep-merge yapıyordu.~~ ✅ Tüm nested config objeleri (`bloom`, `vignette`, `colorGrading`, `ao`) dinamik olarak deep-merge ediliyor (`Object.keys` iterasyonu ile).

**Dosya:** `src/store/useSceneStore.ts`

### 5. ✅ `removeOption` — Index Mantık Hatası → **DÜZELTİLDİ**
~~`useVariantsStore.ts`'de bir option silindiğinde `selectedOptionIndex` güncelleme mantığı yanlış.~~ ✅ Seçili option silinirse öncekine geçilir, son option silinirse `null` olur, index kayması düzgün hesaplanır.

**Dosya:** `src/store/useVariantsStore.ts`

### 6. ✅ `deleteObject` — Texture Dispose Eksik (GPU Memory Leak) → **DÜZELTİLDİ**
~~`useEditorStore.ts`'de obje silinirken geometry ve material dispose ediliyor ama material'e bağlı texture'lar dispose edilmiyor.~~ ✅ `map`, `normalMap`, `roughnessMap`, `metalnessMap`, `aoMap`, `emissiveMap`, `displacementMap`, `alphaMap`, `bumpMap`, `envMap`, `lightMap` — tüm texture prop'ları dispose ediliyor.

**Dosya:** `src/store/useEditorStore.ts`

### 7. ✅ `deleteObject` — Stale Selection Filtresi → **DÜZELTİLDİ**
~~`selectedObjects` filtreleme `obj.uuid !== id` ile yapılıyor, ama `selectObject` fonksiyonu custom `id` kabul edebiliyor.~~ ✅ Artık `selectedObjectIds.filter(sid => sid !== id)` ile custom ID üzerinden doğru filtreleniyor.

**Dosya:** `src/store/useEditorStore.ts`

### 8. ✅ `loadProject` — Race Condition → **DÜZELTİLDİ**
~~`useProjectStore.ts`'deki `loadProject` async ama cancellation mekanizması yok.~~ ✅ `_loadVersion` sayacı eklendi. Yeni bir `loadProject` çağrısı başlarsa eski yükleme sonuçları atılır.

**Dosya:** `src/store/useProjectStore.ts`

### 9. ✅ `loadProject` — Shallow Copy Mutation Bugu → **DÜZELTİLDİ**
~~`assets` objesi spread ile kopyalanıyor ama `assets.models.forEach(model => { model.url = ... })` orijinal model objelerini mutate ediyordu.~~ ✅ `models` dizisi `map(m => ({ ...m }))` ile deep-clone ediliyor.

**Dosya:** `src/store/useProjectStore.ts`

### 10. ✅ `HdriSphere` — Sadece .hdr Destekleniyor → **DÜZELTİLDİ**
~~`.exr` uzantılı dosyalar `RGBELoader`'a yönlendiriliyordu.~~ ✅ `.exr` desteği kaldırıldı. Sadece `.hdr` dosyaları `RGBELoader` ile yüklenir, diğerleri `TextureLoader` ile.

**Dosya:** `src/components/canvas/HdriSphere.tsx`

### 11. ✅ Blob URL'ler Asla Revoke Edilmiyor → **DÜZELTİLDİ**
~~`URL.createObjectURL()` çağrıları sonrasında `URL.revokeObjectURL()` hiçbir zaman çağrılmıyordu.~~ ✅ Aşağıdaki düzeltmeler uygulandı:
- `BlockEditor.tsx`: Yeni görsel yüklenirken eski blob URL revoke ediliyor
- `HotspotPanel.tsx`: İkon değiştirilirken eski blob URL revoke ediliyor (hem node hem default)
- `useProjectStore.ts`: `removeModel` içinde blob URL revoke ediliyor

**Dosyalar:** `src/components/ui/panels/BlockEditor.tsx`, `src/components/ui/panels/HotspotPanel.tsx`, `src/store/useProjectStore.ts`

### 12. ✅ Variant Texture'ları — Memory Leak → **DÜZELTİLDİ**
~~`ModelRenderer.tsx`'deki variant efekti asenkron texture yükleliıyor ama eski texture'ları dispose etmiyordu.~~ ✅ `useEffect` cleanup fonksiyonunda:
- Tüm yüklenen texture'lar dispose ediliyor
- Variant material cache dispose ediliyor
- Orijinal material'ler geri yükleniyor

**Dosya:** `src/components/canvas/ModelRenderer.tsx`

### 13. ✅ `VariantsPanel` — Option Adı Kayıt Edilmiyor → **DÜZELTİLDİ**
~~Edit option modalda ad input'u sadece local state'i güncelliyor, "Kaydet" butonunda yeni isim store'a yazılmıyordu.~~ ✅ `handleSaveOptionEdit` içinde `updateOption(groupId, optionIndex, { name })` çağrısı eklendi.

**Dosya:** `src/components/ui/panels/VariantsPanel.tsx`

### 14. ✅ `MobileJoystick` — Event Listener Sızıntısı → **DÜZELTİLDİ**
~~`useNeedsJoystick` hook'unda `resize` event handler'ı her tetiklendiğinde yeni bir `keydown` listener ekliyor ama temizlemiyor.~~ ✅ `{ once: true }` kullanılıyor, `checkDevice()` içinde return ile cleanup sağlanıyor ve `resize` cleanup uygulanmış.

**Dosya:** `src/components/ui/MobileJoystick.tsx` (L184–218)

---

## 🟡 ORTA — Hatalı Davranış, Tutarsızlık

### 15. ✅ Undo/Redo Tutarsızlığı → **DÜZELTİLDİ**
~~Sadece `useHotspotStore` ve `useVariantsStore` history desteği sunuyordu.~~ ✅ Aşağıdaki store'lara da `useHistoryStore.pushAction` entegrasyonu eklendi:
- **SceneStore**: `updateEnvironment`, `addLight`, `updateLight`, `removeLight`, `updateEffects`
- **InteractionsStore**: `addZone`, `updateZone`, `updateZonePopup`, `removeZone`
- **EditorStore**: `toggleMeshVisibility`
- **EditorControls**: Transform undo zaten mevcuttu ✅

**Dosyalar:** `src/store/useSceneStore.ts`, `src/store/useInteractionsStore.ts`, `src/store/useEditorStore.ts`

### 16. `toggleMeshVisibility` — THREE.js Objelerini Doğrudan Mutate Ediyor
`mesh.object.visible = true/false` atandığında React re-render tetiklemez. Sadece `hiddenMeshIds` state değişikliği re-render sağlar → bazı bileşenler eski görünürlük bilgisini gösterebilir.

**Dosya:** `src/store/useEditorStore.ts` (L249–274)

### 17. THREE.Object3D Referansları Zustand State'inde
`selectedObjects` ve `sceneMeshes` mutable Three.js objeleri. Zustand'ın shallow equality kontrolü property değişikliklerini algılamaz, ayrıca dispose edilmiş objelerin GC'si engellenir.

**Dosya:** `src/store/useEditorStore.ts` (L38)

### 18. ✅ `loadFromConfig` — Eksik Default Değerler → **DÜZELTİLDİ (13.02.2026)**
~~`useSceneStore.ts`'de `loadFromConfig` yüklerken `camera` veya `player` config'de yoksa `undefined` olur.~~ ✅ `effects`, `deletedMeshIds`, `player` ve `camera` — tüm alanlar için `defaultSceneConfig` fallback eklendi.

**Dosya:** `src/store/useSceneStore.ts`

### 19. ✅ Fetch Hataları Sessizce Yutuluyordu → **DÜZELTİLDİ**
~~`useProjectStore.ts`'de `fetch(...).catch(() => null)` ile tüm hatalar sessizce `null`'a dönüştürülüyordu.~~ ✅ Her fetch `.catch()` içinde hata mesajı loglanıyor (`log('loadProject', \`...fetch failed: ${e.message}\`)`).

**Dosya:** `src/store/useProjectStore.ts`

### 20. ✅ `ConfiguratorPanel` — `btoa()` Türkçe Karakterlerde Crash → **DÜZELTİLDİ**
~~`btoa(JSON.stringify(selections))` Türkçe karakter içerirse crash oluyordu.~~ ✅ Encode: `btoa(unescape(encodeURIComponent(...)))`, Decode: `decodeURIComponent(escape(atob(...)))` kullanılıyor.

**Dosya:** `src/components/ui/ConfiguratorPanel.tsx`

### 21. ✅ `NodeNavigationControls` — Her Frame'de State Güncellemesi → **DÜZELTİLDİ**
~~`setCursorPosition(groundPoint)` her frame'de yeni `Vector3` ile çağrılıyor → her frame React state set ediliyor → sürekli re-render.~~ ✅ `cursorPosition` `useState`'den `useRef`'e çevrildi. `cursorPositionRef.current.copy(groundPoint)` ile güncelleniyor — state güncellemesi ve re-render yok.

**Dosya:** `src/components/canvas/NodeNavigationControls.tsx`

### 22. ✅ `InteractionZone` — Label Pozisyon Bugu → **DÜZELTİLDİ**
~~Group `scale={[radius, radius, radius]}` ile ölçekleniyor, label pozisyonu `[0, radius + 0.3, 0]` (local space). `radius=5` için label `y=26.5`'te.~~ ✅ Label pozisyonu `[0, 1 + 0.3 / radius, 0]` olarak düzeltildi → world space'de her zaman kürenin 0.3 birim üstünde.

**Dosya:** `src/components/canvas/InteractionZone.tsx`

### 23. ✅ `BackgroundAudio` — `src` Değişince Çalmaz → **DÜZELTİLDİ**
~~`hasStartedRef.current` bir kez `true` olduktan sonra sıfırlanmıyordu. `src` değişince yeni audio asla auto-play olmuyordu.~~ ✅ `useEffect` cleanup'ında `hasStartedRef.current = false` eklendi. `src`, `loop` veya `volume` değiştiğinde yeni audio ilk etkileşimde otomatik çalar.

**Dosya:** `src/components/ui/BackgroundAudio.tsx`

### 24. ✅ `GroundCursor` — Lerp vs JSX Position Çakışması → **DÜZELTİLDİ**
~~Mesh'e hem JSX'te `position={position}` veriliyor hem de `useFrame`'de `lerp` uygulanıyor → JSX her render'da pozisyonu sıfırlıyordu.~~ ✅ JSX'teki `position={position}` prop'u kaldırıldı. Pozisyon yalnızca `useFrame` içindeki `lerp` ile kontrol ediliyor.

**Dosya:** `src/components/canvas/GroundCursor.tsx`

### 25. ✅ Keyboard Shortcut Tooltip'leri Yanlış → **DÜZELTİLDİ**
~~`EditorPage.tsx` toolbar butonları `"Taşı (W)"` diyor ama asıl kısayol `'g'` idi.~~ ✅ `W` tuşu da translate kısayoluna eklendi. Tooltip'ler doğru (W, E, R).

**Dosya:** `src/pages/EditorPage.tsx`

### 26. ✅ `PlayerSettingsPanel` — Dinamik Tailwind Class'ları Çalışmıyor → **DÜZELTİLDİ**
~~`` `focus:border-${color}-400` `` şeklinde dinamik class oluşturuluyordu.~~ ✅ Statik class dizisi kullanılıyor: `['focus:border-red-400', 'focus:border-green-400', 'focus:border-blue-400'][i]` — Tailwind JIT tarafından algılanır.

**Dosya:** `src/components/ui/panels/PlayerSettingsPanel.tsx`

### 27. ✅ `PropertiesPanel` — Doğrudan Three.js Obje Mutasyonu → **DÜZELTİLDİ**
~~`selectedObject!.position.x = v` ile Three.js objesi doğrudan değiştiriliyor, React re-render tetiklenmiyordu.~~ ✅ Her mutation sonrasında `forceUpdate()` (`useReducer`) çağrılıyor → tüm input alanları güncel değerleri gösteriyor.

**Dosya:** `src/components/ui/panels/PropertiesPanel.tsx`

### 28. ✅ `Octree.ts` — Division by Zero → **DÜZELTİLDİ**
~~`Math.abs(d1) / (Math.abs(d1) + Math.abs(d2))` — `d1 === 0 && d2 === 0` iken payda sıfır → `NaN`.~~ ✅ Payda `0` ise `t = 0.5` (kapsül orta noktası) kullanılıyor.

**Dosya:** `src/utils/Octree.ts`

### 29. ✅ `useHistory` — `null` Sentinel Değer Çakışması → **DÜZELTİLDİ**
~~`previousValue.current === null` "değişiklik yok" anlamında kullanılıyordu, gerçek `null` değerler undo yapılamıyordu.~~ ✅ Ayrı `hasStarted` boolean ref eklendi. `null` değerli property'ler de artık undo/redo edilebilir.

**Dosya:** `src/hooks/useHistory.ts`

### 30. ✅ `index.html` — Yanlış Favicon Yolu → **DÜZELTİLDİ**
~~`href="/public/favicon.ico"` — Vite'da `public/` dizini zaten root'a servis edilir, production'da 404 dönüyordu.~~ ✅ `href="/favicon.ico"` olarak düzeltildi.

**Dosya:** `index.html`

### 31. ✅ GSAP Tween Cleanup Yok → **DÜZELTİLDİ**
~~`NodeNavigationControls.tsx`'de GSAP animasyonu unmount'ta temizlenmiyordu.~~ ✅ Cleanup'a `gsap.killTweensOf(camera.position)` eklendi. Component unmount edilirse animasyon durur.

**Dosya:** `src/components/canvas/NodeNavigationControls.tsx`

### 32. ✅ `EditorControls` — Undo/Redo Stale Object Referansı → **DÜZELTİLDİ**
~~Undo/redo closure'ları silinmiş Three.js obje referansını yakalar, ölü referans üzerinden işlem yapardı.~~ ✅ Undo/redo fonksiyonlarının başında `objectRef.parent` kontrolü eklendi. Obje sahnede yoksa işlem atlanır ve uyarı loglanır.

**Dosya:** `src/components/canvas/EditorControls.tsx`

### 33. ✅ Store Barrel Export Eksikleri → **DÜZELTİLDİ**
~~`src/store/index.ts`'den `useHotspotStore` ve `useToastStore` re-export edilmiyordu.~~ ✅ Her iki store da barrel export'a eklendi.

**Dosya:** `src/store/index.ts`

---

## 🔵 DÜŞÜK — Performans, Kod Kalitesi, İyileştirme

### EK: ✅ `EditorGate` — Şifre Girişi Yönlendirmiyor → **DÜZELTİLDİ (12.02.2026)**
`App.tsx`'deki `EditorGate` bileşeninde `isAuthorized`, `useMemo` ile hesaplanıyordu ama `localStorage`'a yazıldıktan sonra re-render tetiklenmiyordu. Şifre doğru olsa bile sayfa yenilenene kadar editor açılmıyordu. ✅ `authorized` state eklenerek çözüldü.

**Dosya:** `src/App.tsx`

### EK: ✅ `Environment Intensity` — Özel HDR'de Çalışmıyor → **DÜZELTİLDİ (12.02.2026)**
`environment.intensity` slider değeri store'a yazılıyordu ama `<Environment>` component'lerine `environmentIntensity` prop'u geçilmiyordu. ✅ Tüm Environment component'lerine `environmentIntensity` ve `backgroundBlurriness` prop'ları eklendi.

**Dosya:** `src/pages/EditorPage.tsx`, `src/components/ViewerContent.tsx`

### 34. Frame-başı Object Allocation (~15-25 obje/frame)
Aşağıdaki dosyalarda her frame'de `new THREE.Vector3()`, `new THREE.Color()`, `new THREE.Quaternion()` gibi objeler oluşturuluyor (toplam ~1500 allocation/saniye @60fps):
- `FlyControls.tsx` — 5-6 Vector3/frame
- `PlayerControls.tsx` — 4-5 Vector3/frame
- `NodeNavigationControls.tsx` — 1-2 Vector3/frame
- `SelectionHighlight.tsx` — 1 Color/frame
- `ViewerContent.tsx` (CameraTracker) — 1 Vector3/frame
- `LightingManager.tsx` — Vector3 + Quaternion/render

GC baskısı yaratır, jank'a neden olabilir.

### 35. `SimpleOctree` — Gerçek Octree Değil, O(n) Collision
İsmine rağmen spatial subdivision yok. `capsuleIntersect` tüm üçgenleri lineer tarar. 100K+ üçgenli modellerde ciddi performans düşüşü.

**Dosya:** `src/utils/Octree.ts`

### 36. Tek Büyük Vendor Chunk
`vite.config.ts`'de tüm `node_modules` tek bir `vendor` chunk'a konuyor. Three.js (~600KB+), React, Zustand, JSZip hep birlikte → ilk yükleme yavaş, code-splitting avantajı kaybediliyor.

**Dosya:** `vite.config.ts` (L18–22)

### 37. ✅ Tutarsız ID Üretimi → **DÜZELTİLDİ (13.02.2026)**
~~Store'lar arası tutarsız ID üretimi: `nanoid`, `Math.random().toString(36)`, deprecated `substr` kullanılıyordu.~~ ✅ Tüm store'lar `nanoid` kullanıyor. `nanoid` doğrudan bağımlılık olarak eklendi. `useToastStore`'daki `substr` → `substring` düzeltildi.
- `useHotspotStore`: `nanoid` ✅
- `useInteractionsStore`: `nanoid` ✅
- `useVariantsStore`: `nanoid` ✅
- `useSceneStore`: `nanoid` ✅
- `useToastStore`: `substring` ✅

### 38. `VariantOption` — `id` Alanı Yok
Option'lar yalnızca array index'i ile tanımlanıyor. History işlemleri ve serileştirme için kırılgan.

### 39. ✅ `addDeletedMesh` — Duplicate Kontrolü Yok → **DÜZELTİLDİ (13.02.2026)**
~~`useSceneStore.ts`'de aynı mesh ID ile birden çok kez çağrılabilir, array sınırsız büyüyordu.~~ ✅ `includes(id)` kontrolü eklendi — aynı ID tekrar eklenmez.

### 40. ✅ `HotspotRenderer` — Her Node İçin useFrame → **DÜZELTİLDİ (13.02.2026)**
~~Her `HotspotNodeMesh` kendi `useFrame` callback'ini register ediyordu. 50 hotspot = 50 useFrame çağrısı/frame.~~ ✅ Per-node `useFrame` kaldırıldı, parent `HotspotRenderer`'a tek bir `useFrame` taşındı. Ref map ile sadece seçili node'un pozisyonu senkronize ediliyor.

### 41. ✅ Erişilebilirlik (Accessibility) Sorunları → **DÜZELTİLDİ (13.02.2026)**
~~Dialog'lar, toast, butonlar ve loading screen'de WCAG uyumluluğu eksikti.~~ ✅ Düzeltmeler:
- `NewProjectDialog`: `role="dialog"`, `aria-modal`, `aria-labelledby`, Escape tuşu eklendi
- `ViewerPreviewModal`: `role="dialog"`, `aria-modal`, `aria-label`, Escape tuşu, close button `aria-label` eklendi
- `ToastContainer`: Container'a `role="status"` + `aria-live="polite"`, her toast'a `role="alert"`, close button'a `aria-label` eklendi
- `BackgroundAudio`: Buton'a `aria-label` eklendi
- `ControlsInfo`: Collapsed ve close butonlarına `aria-label` eklendi
- `LoadingScreen`: Container'a `role="status"` + `aria-live="polite"` eklendi

### 42. Global Window Mutasyonları (Type-unsafe)
Aşağıdaki global property'ler `window as any` ile atanıyor, TypeScript deklarasyonu yok:
- `window.__loadedModelFiles`
- `window.__loadedTextures`
- `window.__interactionFiles`
- `window.__blobUrlToFileName`
- `window.__dataUrlToFileName`

### 43. `VariantsPanel` — 1541 Satır Tek Bileşen
Aşırı büyük bileşen, 17+ `useState` çağrısı. `adjustBrightness` fonksiyonu 2 kez tanımlı (L294 ve L1530).

### 44. ✅ `ViewerPage.tsx` — `projectId` Undefined Olabilir → **DÜZELTİLDİ (13.02.2026)**
~~`useParams<{ projectId: string }>()` — `projectId` `undefined` dönebilir ama null check yoktu.~~ ✅ `projectId` yoksa `'/'`'e redirect yapılıyor, render'da `null` döndürülüyor.

### 45. ✅ `HomePage.tsx` — Eksik WebGL Cleanup → **DÜZELTİLDİ (13.02.2026)**
~~Three.js renderer ve material dispose ediliyor ama `PlaneGeometry` dispose edilmiyor, mesh scene'den kaldırılmıyordu.~~ ✅ Cleanup'ta `scene.remove(mesh)`, `mesh.geometry.dispose()` eklendi.

### 46. ✅ `HomePage.tsx` — Proje Listesi Hardcoded → **DÜZELTİLDİ (13.02.2026)**
~~`AVAILABLE_PROJECTS = ['1108-1', 'demo']` statik. `public/data/`'ya eklenen yeni projeler otomatik görünmüyordu.~~ ✅ `public/data/projects.json` manifest dosyası oluşturuldu, `HomePage` başlangıçta bu dosyadan proje listesini dinamik olarak fetch ediyor. Fallback olarak hardcoded liste korunuyor.

---

## 📊 Önceki Test Raporundan Bilinen Sorunlar (Problems.md)

| Sorun | Durum |
|-------|-------|
| R tuşu (Transform Rotate) çalışmıyor | ✅ Düzeltildi — W=Taşı, E=Döndür, R=Ölçekle |
| Undo/Redo çalışmıyor | ✅ Düzeltildi — SceneStore, InteractionsStore, EditorStore entegre edildi |
| Çoklu seçim (Ctrl+Click) paneli tetiklemiyor | Hala Açık |
| Varyant grubu isimlendirme tepki vermiyor | ✅ Düzeltildi — Option adı artık store'a yazılıyor |
| Varyant grubu silme butonu yok | Hala Açık |
| Proje kaydetme butonu yok, Ctrl+S bildirimi yok | Hala Açık |

---

## 📈 Özet İstatistik

| Önem Derecesi | Sayı | Düzeltilen |
|---------------|------|------------|
| 🔴 Kritik (Derlenmez/Çalışmaz) | 1 dosya (5 hata) | ✅ 1 (tamamı) |
| 🟠 Yüksek (Crash/Veri Kaybı/Memory Leak) | 14 | ✅ 13 |
| 🟡 Orta (Hatalı Davranış) | 19 | ✅ 18 |
| 🟢 Editor Şifre ve Env Intensity (ek) | 2 | ✅ 2 |
| 🔵 Düşük (Performans/Kod Kalitesi) | 13 | ✅ 8 |
| **Toplam** | **~51 sorun** | **✅ ~42 düzeltildi** |

---

*Bu rapor, tüm kaynak dosyaların otomatik statik analizi ile oluşturulmuştur. 11.02.2026*
*Son güncelleme: 12.02.2026 — Düzeltmeler: zipExporter tam yeniden yazıldı, circular import çözüldü, removeOption index mantığı düzeltildi, deleteObject texture dispose eklendi, loadProject race condition çözüldü, loadProject shallow copy mutation düzeltildi, deleteObject selection düzeltildi, MobileJoystick listener sızıntısı giderildi, EditorGate şifre girişi düzeltildi, Environment Intensity prop eklendi, keyboard shortcuts W/E/R eklendi, HdriSphere sadece .hdr desteği, undo/redo tüm store'lara entegre edildi, blob URL revoke eklendi, variant texture memory leak düzeltildi, option adı kaydetme düzeltildi, updateEffects tüm nested deep-merge, fetch hataları loglanıyor, btoa Türkçe karakter desteği eklendi.*
*Güncelleme: 13.02.2026 — Faz 1 düzeltmeleri: DEBUG=false (4 dosya), addDeletedMesh duplicate kontrolü, loadFromConfig player/camera default eklendi, ViewerPage projectId null guard, HomePage WebGL geometry+mesh cleanup, proje listesi dinamik fetch (projects.json), tüm store'larda nanoid ile tutarlı ID üretimi, useToastStore substr→substring düzeltildi.*
*Güncelleme: 13.02.2026 (Faz 2) — HotspotRenderer per-node useFrame → tek parent useFrame + ref map optimizasyonu. Accessibility: NewProjectDialog/ViewerPreviewModal role="dialog"+aria-modal+Escape, ToastContainer role="alert"+aria-live, BackgroundAudio/ControlsInfo aria-label, LoadingScreen role="status".*
