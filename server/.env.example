# Backend
PORT=3001
PROJECTS_STORAGE_PATH=/var/www/gangedreality/projects
API_EDITOR_PASSWORD=changeme_strong_password
JWT_SECRET=changeme_random_secret_key_64chars
CORS_ORIGIN=https://yourdomain.com

# Frontend (build time — .env.production olarak kullan)
# VITE_API_BASE_URL=/api
